'use client';

import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

export default function AdminReportsPage() {
  return (
    <PlaceholderPage
      title="Institutional Reports"
      description="Generate and export school-wide performance reports. Scheduled in Phase 2 of the roadmap."
      roles={['ADMIN']}
    />
  );
}
